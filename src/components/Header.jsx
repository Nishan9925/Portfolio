import Navbar from "./NavBar";
import { HeaderTag, HeaderTitle } from "../assets/styles/header/header";

function Header() {
  return (
    <HeaderTag>
      <HeaderTitle>NZ</HeaderTitle>
      <Navbar />
    </HeaderTag>
  );
}

export default Header;
